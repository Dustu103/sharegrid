# Dev — lightweight Node.js static server for hot-reloading frontend
FROM node:20-alpine
WORKDIR /app
COPY package.json ./
RUN npm install
COPY . .
EXPOSE 5173
CMD ["npx", "serve", ".", "--listen", "tcp://0.0.0.0:5173", "--single"]

{
  "name": "sharegrid-frontend",
  "version": "1.0.0",
  "description": "ShareGrid frontend — Vanilla HTML/CSS/JS with Canvas API",
  "private": true,
  "scripts": {
    "dev":   "npx --yes serve . --listen tcp://0.0.0.0:5173 --single",
    "start": "npx --yes serve . --listen tcp://0.0.0.0:5173 --single"
  },
  "keywords": ["canvas", "realtime", "game"],
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
